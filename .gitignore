# Dependencies (there are none, but never commit them anyway)
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml

# Credentials & secrets — NEVER commit these
*.creds.json
credentials/
woolworths-mobile.json
.env
.env.*

# Git worktrees (kept inside the repo)
.worktrees/

# OS / editor cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Scratch
*.log
tmp/
/tmp/
.worktrees/
.worktrees
